import {
  ADD_NOTIFICATION_ID
} from '../actions/types';

const initialState = {
  notificationIDs: []
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
